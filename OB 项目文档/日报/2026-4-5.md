# 日报 · 2026/4/5

## 今日沟通要点

- 搭建本地开发环境（PostgreSQL 16 + Redis）
- Seth 完成 NestJS 后端代码，写入磁盘，编译通过
- 数据库表结构初始化完成
- 明确了本地服务的持久化机制

---

## 关键进展

### 本地开发环境搭建完成

| 组件 | 版本 | 状态 |
|------|------|------|
| Node.js | v25.8.0 | 已有 |
| PostgreSQL | 16.13 | 今日安装，已设开机自启 |
| Redis | 8.6.2 | 今日安装，已设开机自启 |

- 数据库 `ob_dev` 已建，`users` 和 `avatar_jobs` 两张表已初始化
- PostgreSQL / Redis 已设开机自启（`brew services enable`），以后开机自动运行无需手动操作

### Seth：NestJS 后端代码写入磁盘

项目路径：`/Users/liyudong/Desktop/OB project/ob-server/`

完成模块：

| 模块 | 文件 | 说明 |
|------|------|------|
| 根 | `src/main.ts` | 启动入口，全局异常过滤器 |
| 根 | `src/app.module.ts` | TypeORM / Redis / 全局 Guard 注册 |
| 公共 | `src/common/snowflake.service.ts` | Snowflake UID 生成（格式 `ob_{id}`） |
| 公共 | `src/common/redis.decorator.ts` | `@InjectRedis()` 装饰器 |
| 认证 | `src/auth/auth.controller.ts` | `POST /auth/wechat/login` |
| 认证 | `src/auth/auth.service.ts` | 登录主流程 + Redis 分布式锁 |
| 认证 | `src/auth/wechat.service.ts` | 微信 code2Session（含 mock 模式） |
| 认证 | `src/auth/jwt.strategy.ts` | Passport JWT Strategy |
| 认证 | `src/auth/guards/jwt-auth.guard.ts` | 全局 JWT 验证 |
| 认证 | `src/auth/guards/pending-profile.guard.ts` | 状态机拦截，DB 实时查状态 |
| 用户 | `src/users/users.controller.ts` | `GET /users/me`、`POST /users/me/profile`、`GET /users/me/friend-requests` |
| 用户 | `src/users/users.service.ts` | 用户 CRUD + 状态转换 |
| 用户 | `src/users/entities/user.entity.ts` | TypeORM Entity |
| 头像 | `src/avatar/avatar-job.service.ts` | 头像任务入库（Worker 后续实现） |

**关键设计**：
- mock 模式：`WECHAT_APP_ID` 含 `mock` 字样时自动启用，code 用 `mock_` 开头即可测试
- 并发保护：Redis SET NX EX 分布式锁 + Lua 脚本原子释放
- 状态校验：Guard 每次请求实时查 DB，不信任 JWT 中的 status
- 幂等性：openid 唯一索引 + 事务内双重检查

`npm run build` 编译通过，无 TypeScript 错误。

### 启动命令备忘

```bash
# 启动 NestJS（开发热重载）
cd "/Users/liyudong/Desktop/OB project/ob-server" && npm run start:dev

# PostgreSQL / Redis 开机自启已配置，无需手动启动
```

---

## 遇到的问题与解决

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| Redis 第一次 brew install 下载失败 | 网络问题（ghcr.io 不稳定） | 重新执行 `brew install redis` |
| PostgreSQL brew install 下载失败 | 同上 | 重新执行后成功 |

---

## 待办 Todo

### 立即可做
- [ ] **验证后端接口**：启动 NestJS（`npm run start:dev`），用 curl 跑一遍登录→填资料→获取用户信息的完整链路
- [ ] **前后端联调**：iOS 客户端目前 mock 数据，需对接本地 NestJS 接口（修改 `AuthStateManager` 调用真实 API）

### 产品待确认（需用户拍板）
- [ ] 资料提交成功后，status 是**立即**改为 ACTIVE，还是等好友引导页点「下一步」之后再改？（目前客户端按立即改处理）
- [ ] 登录失败时是否需要 Toast 提示？
- [ ] 好友引导页占位图资源（需 Gemini 提供）

### 后续开发（注册登录 Step 5 测试）
- [ ] **Rock 启动测试**：后端接口跑通后，Rock 按 50 条测试用例逐一验证
- [ ] **BullMQ Worker**：头像 CDN 异步任务 Worker，本期建了结构，下期实现

### 后续模块
- [ ] 下一个 PRD 模块开发（拍照流程 / 审核流程，等 Gemini 产出）
- [ ] Figma 设计稿对接（等 Stitch AI 产出）
